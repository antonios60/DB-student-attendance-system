# DB-student-attendance-system
is a SQL Server database that manages students, courses, instructors, enrollments, sessions, and attendance records. It tracks attendance, prevents duplicate entries, calculates attendance percentages, and generates reports using tables, relationships, views, triggers, and stored procedures.
